<?xml version="1.0" encoding="utf-8" ?>
<Shell xmlns="http://xamarin.com/schemas/2014/forms"
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
       xmlns:syncblackduck="clr-namespace:SyncBlackDuck"
       xmlns:views="clr-namespace:SyncBlackDuck.Views"
       x:DataType="syncblackduck:AppShell"
       x:Class="SyncBlackDuck.AppShell">

    <FlyoutItem Title="Main" >
        <ShellContent Route="MainPage" ContentTemplate="{DataTemplate views:MainPage}" />
    </FlyoutItem>
    
    <FlyoutItem Title="Admin" > 
        <ShellContent Route="AdminMainPage" ContentTemplate="{DataTemplate local:AdminMainPage}" />
    </FlyoutItem>

    <!-- When the Flyout is visible this will be a menu item you can tie a click behavior to  -->
    <MenuItem Text="Logout" StyleClass="MenuItemLayoutStyle" Clicked="OnMenuItemClicked">
    </MenuItem>

    <TabBar>
        <ShellContent Route="MainPage" ContentTemplate="{DataTemplate local:MainPage}" />
    </TabBar>

</Shell>